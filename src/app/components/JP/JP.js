import React, { useRef, useEffect } from 'react';
import Particles from "./Particles";
import "./styles.css";

const JP = () => {
    return (
        <div className="App">
          <Particles />
        </div>
      );
};

export default JP;